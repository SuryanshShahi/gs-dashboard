import PageWrapper from "@/components/pageWrapper";
import Settings from "@/features/settings";

const Page = () => {
  return (
    <PageWrapper>
      <Settings />
    </PageWrapper>
  );
};

export default Page;
