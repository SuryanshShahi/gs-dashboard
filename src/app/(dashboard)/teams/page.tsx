import PageWrapper from "@/app/components/pageWrapper";
import Teams from "@/app/features/teams";

const Page = () => {
  return (
    <PageWrapper>
      <Teams />
    </PageWrapper>
  );
};

export default Page;
