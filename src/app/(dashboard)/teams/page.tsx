import PageWrapper from "@/components/pageWrapper";
import Teams from "@/features/teams";

const Page = () => {
  return (
    <PageWrapper>
      <Teams />
    </PageWrapper>
  );
};

export default Page;
