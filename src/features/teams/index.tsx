import PageHeader from "@/shared/heading/PageHeader";

const Teams = () => {
  return (
    <div>
      <PageHeader
        titleProps={{
          children: "Teams",
        }}
        descriptionProps={{
          children: "Manage your team and collaborate with your team members.",
        }}
      />
    </div>
  );
};

export default Teams;
